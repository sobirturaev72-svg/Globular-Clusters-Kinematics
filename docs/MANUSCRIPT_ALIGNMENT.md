# Manuscript alignment

Associated article title:

**Projected Kinematic Anisotropy and Velocity-Dispersion Gradients in Galactic Globular Clusters: An Interval-Defined Framework from HST Proper Motions**

The release follows the article's final numerical hierarchy:

1. `R <= R_h,l` is the primary comparison.
2. Full HST range is a radial-coverage sensitivity layer.
3. KAP and GKI classes use analytic, cluster-specific two-sided 2-sigma thresholds.
4. `D_s` and `D_s,corr` are profile-inspection diagnostics only.
5. NGC 6388 uses the audited 31-bin full profile and 13 primary bins.
6. Figure B3 is the KAP-normalized-anisotropy identity over the full HST range.
7. Figure B4 is the full-versus-primary radial-interval sensitivity comparison.
8. Table A4 uses the notation `-<beta_norm>_wS`, where `S_i=sigma_r^2+sigma_t^2`.

The article PDF and source are excluded from the DOI archive. The machine-readable
claim crosswalk is `MANUSCRIPT_NUMERICAL_CROSSWALK.csv`.
